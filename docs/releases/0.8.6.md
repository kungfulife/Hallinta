## Interface

- Replaced unsupported arrow glyphs with clear portable labels in every sort option.

## Save Monitor

- Starting automatically with Hallinta or a detected Noita process now creates a generated-name session immediately.
- Automatic starts no longer show stopped-session selection or naming prompts.
- Manual starts still offer resume, new-session, and naming choices.

## Workshop Detection

- Steam and every registered Steam library are now discovered automatically when checking Workshop content.
- Removed the unnecessary Steam path setting. Existing settings files continue to load without migration.
